# Installation

1. Run
    ```bash
    apt update -y && apt install git -y && git clone https://github.com/hmnid/beer-node.git /opt/remnanode && cd /opt/remnanode
    ```
2. Run `cp .env.sample .env` and specify all settings
3. Run `./install.bash`
